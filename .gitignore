# Repo-wide safety nets; each site carries its own .gitignore as well.
node_modules/
dist/
.vite/
.DS_Store
*.local
tsconfig.tsbuildinfo
# Raw pipeline downloads of any site — large, re-fetchable, never committed.
**/data/raw/
